
Build the iam app using the ./build.sh command. 
Before you can run the app using the static lib, you need to add it to your env variables, e.g.:
export $LD_LIBRARY_PATH:/home/user/workdir/bacnet-stack/_build

